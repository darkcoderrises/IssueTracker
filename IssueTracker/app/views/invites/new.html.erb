<h1>New Invite</h1>

<%= form_for(:invite) do |f| %>
  <%= f.text_field :project_id, :placeholder => "Project Id" %>
  <%= f.text_field :user_id, :placeholder => "User Email" %>
  <%= f.submit "Create New", class: "btn-submit" %>
<% end %>

<%= link_to 'Back', invites_path %>
