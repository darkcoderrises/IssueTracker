<h1>New Invite</h1>

<%= form_for(:invite) do |f| %>
<table>
<tr><td>Project_id:</td><td><%= f.text_field :project_id %></td></tr>
<tr><td>Issue_id:</td><td><%= f.text_field :user_id %></td></tr>
</table>
  <%= f.submit "Create New", class: "btn-submit" %>
<% end %>

<%= link_to 'Back', invites_path %>
