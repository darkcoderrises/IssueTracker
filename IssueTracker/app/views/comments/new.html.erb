<h1>New Comment</h1>

<%= form_for(:comment) do |f| %>
  <%= f.text_area :comment, :placeholder => "Comment" %>

  <%= f.submit "Create New", class: "btn-submit" %>
<% end %>


<%= link_to 'Back', comments_path %>
